{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local pages (there are none) get the core set.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
