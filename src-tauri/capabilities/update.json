{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "update",
  "description": "The update offer window (ui/update.html). It may ask for the update to be installed or snoozed, and listen for progress. Nothing else.",
  "windows": ["update"],
  "permissions": ["core:default", "allow-install-update", "allow-snooze-update"]
}
